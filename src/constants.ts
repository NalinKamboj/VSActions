"use strict";

export const extensionId = "vsactions";
export const extensionOutputChannelName = "VSActions";
export const extensionQualifiedId = `nalinkamboj.${extensionId}`;

// TODO Maybe explore using this enum?

export enum WorkspaceState {
  DefaultRemote = "vsactions:remote:default",
}
